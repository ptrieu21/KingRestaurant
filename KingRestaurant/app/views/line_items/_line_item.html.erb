<tbody>
	<tr>
		<td><%= line_item.food_item.name %></td>
		<td class="text-center"><%= line_item.quantity %></td>
		<td><%= number_to_currency(line_item.food_item.price, locale: :vi) %></td>
		<td><%= number_to_currency(line_item.total_price, locale: :vi) %></td>
	</tr>
</tbody>